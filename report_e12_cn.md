# E12: 基于三元组差分编码的流水线并行激活值压缩方案

> **模型**: Qwen2.5-32B-Instruct (320亿参数, hidden\_dim = 5120)
> **层切分边界**: 第6层 (前6个Transformer层位于PP stage 0)
> **评估规模**: 7个数据集 × 2种上下文长度 (512, 2048) — 每种组合50次预热 + 50次测试请求
> **硬件**: 8 × NVIDIA A800-SXM4-80GB

---

## 1. 引言与动机

在大语言模型的流水线并行 (Pipeline Parallelism, PP) 推理中，中间激活值需要在每个micro-batch边界处跨流水线阶段传输。对于一个320亿参数的模型，在第6层切分处 (hidden\_dim = 5120)，单个2048 token的请求需要传输 **20 MB** 的FP16数据。当PP阶段部署在通过带宽受限链路 (200 Mbps–1 Gbps) 连接的不同节点上时，该传输成为显著的性能瓶颈。

我们提出了一种**三元组差分编码流水线 (Trigram Delta-Coding Pipeline)**，利用以下观察：n-gram token模式在不同请求间频繁重复出现。通过维护一个持久化的DAG字典树来缓存以token三元组为键的历史激活值，我们可以将大部分位置表示为相对于缓存参考值的小增量，实现 **2.5–3.0倍压缩**，**余弦损失 < 0.04%**。

---

## 2. 系统设计

### 2.1 架构概览

```
┌────────────────────────────────────────────────────────────────┐
│  发送端 (PP Stage 0)                                            │
│                                                                │
│  ① Prefill前向传播 (GPU) ──────────┐                            │
│  ② 分类 (CPU, 与①重叠执行)         │  并发执行                   │
│  ③ 分层编码 (GPU):                 │                            │
│     • 三/二元组 → 仿射变换 + Int4差分 + Top-K稀疏              │
│     • 自引用 → 相对于已重建位置的差分                           │
│     • 一元组 → Int8分组量化 + 离群值Top-K                      │
│  ④ 表更新 (CPU, 与③重叠执行)                                   │
└────────────────────────────────────────────────────────────────┘
      │ 压缩数据包 (缩小2.5–3.0倍)
      ▼
┌────────────────────────────────────────────────────────────────┐
│  接收端 (PP Stage 1)                                            │
│  ⑤ 解码 + 重建激活值                                            │
└────────────────────────────────────────────────────────────────┘
```

### 2.2 分层体系 (级联回退)

每个token位置按优先级被分类到以下四个层级之一：

| 层级 | 匹配条件 | 编码方式 | 说明 |
|------|----------|----------|------|
| **三元组 (Trigram)** | DAG字典树中精确匹配 (A,B,C) | 仿射变换 + Int4差分 | 最佳参考——3-token上下文前缀匹配历史模式 |
| **二元组 (Bigram)** | DAG中匹配 (B,C) 前缀 | 仿射变换 + Int4差分 | 2-token上下文匹配；参考稍弱 |
| **自引用 (Self-ref)** | 同一请求中出现过相同三元组 | 仿射变换 + Int4差分 | 相对于同一请求中已重建位置的差分 |
| **一元组 (Unigram)** | 无匹配 | Int8 + 离群值Top-K | 完全独立编码；无可用参考 |

### 2.3 DAG字典树存储

隐藏状态以两级字典树 (trie) 组织：

```
dag[token_A][token_B] → BigramNode:
    .bigram_hidden  = 位置B的隐藏状态 (2-token上下文)
    .suffixes[token_C] = 位置C的隐藏状态 (3-token上下文)
```

该结构在所有具有相同 (A, B) 前缀的三元组之间共享二元组前缀查找，减少查找开销和内存占用。

### 2.4 编码细节

- **仿射变换**: 逐位置的缩放因子和偏置，将参考激活值对齐到真实激活值
- **Int4差分**: 仿射对齐后残差的分组4位量化 (group\_size = 128)
- **Top-K稀疏**: 每组保留top-1离群值以FP16存储，保持精度
- **Int8一元组**: 对于无参考的位置，使用分组8位量化 + top-1 FP16离群值编码

### 2.5 CPU/GPU重叠优化

两个CPU密集型操作——**分类** (DAG字典树查找) 和**表更新** (DAG字典树插入)——通过后台 `ThreadPoolExecutor` 与GPU工作重叠执行：

- `classify_and_build_refs` 与GPU prefill前向传播并发执行 (Python GIL在CUDA kernel启动期间释放)
- `update_from_hidden_states` 与GPU编码阶段 (Phase 3a–3c) 并发执行
- 批量数据类型转换 (一次性 `hidden_states.to(fp16)` 替代逐位置转换) 进一步降低更新路径的CPU开销

两个CPU操作完全隐藏在GPU工作之后，仅贡献 **< 0.15 ms** 可见延迟。

---

## 3. 实验设置

| 参数 | 值 |
|------|-----|
| 模型 | Qwen2.5-32B-Instruct (bfloat16) |
| 隐藏维度 | 5120 |
| 层切分边界 | 6 |
| 量化分组大小 | 128 |
| Top-K稀疏度 | 每组1个 |
| Int8分组大小 | 128 |
| Int8离群值top-K | 1 |
| 表模式 | 冷启动 (空表) |
| 表更新模式 | 序列提取 (复用prefill隐藏状态) |
| 预热请求数 | 50 (表填充阶段，不计入指标) |
| 测试请求数 | 50 (仅记录这些请求的指标) |
| 上下文长度 | 512, 2048 tokens |

**数据集** (7个多样化语料库)：

| 数据集 | 领域 |
|--------|------|
| wikitext-2 | 百科文本 |
| CNN/DailyMail | 新闻文章 |
| XSum | 新闻摘要 |
| Alpaca | 指令跟随 |
| GSM8K | 数学应用题 |
| TriviaQA | 问答 |
| ShareGPT | 多轮对话 |

每个 (数据集, 上下文长度) 组合使用**唯一、不重叠的提示词**进行预热和测试，确保测试阶段衡量的是真实的缓存泛化能力——而非记忆数据。

---

## 4. 分层匹配率

经过50次预热请求后，DAG字典树包含数万个三元组条目。下表展示测试阶段各位置被分类到每个层级的百分比：

| 层级 | ctx = 512 | ctx = 2048 |
|------|-----------|------------|
| 三元组 | 17.0% ± 11.8% | 24.5% ± 17.8% |
| 二元组 | 21.8% ± 6.0% | 22.2% ± 7.5% |
| 自引用 | 9.4% ± 10.2% | 19.3% ± 14.0% |
| 一元组 | 51.7% ± 13.0% | 34.0% ± 12.5% |
| **总覆盖率** | **48.3%** | **66.0%** |

ctx = 2048时覆盖率更高，因为更长的序列包含更多重复的n-gram模式 (自引用层级从9.4%增长到19.3%)。

### 各数据集匹配率 (ctx = 2048)

| 数据集 | 三元组% | 二元组% | 自引用% | 一元组% | 覆盖率% |
|--------|---------|---------|---------|---------|---------|
| ShareGPT | 15.6 | 13.9 | 33.3 | 37.1 | 62.9 |
| Alpaca | 12.7 | 17.6 | 41.7 | 27.9 | 72.1 |
| CNN/DM | 12.2 | 29.4 | 10.7 | 47.8 | 52.3 |
| GSM8K | 41.2 | 24.7 | 10.4 | 23.8 | 76.2 |
| TriviaQA | 54.3 | 13.3 | 11.7 | 20.8 | 79.2 |
| wikitext | 22.2 | 27.6 | 13.2 | 37.1 | 62.9 |
| XSum | 13.1 | 28.6 | 14.5 | 43.8 | 56.2 |

**观察**: 结构化/重复性领域 (GSM8K, TriviaQA) 实现76–79%覆盖率，而多样化自由文本 (CNN/DM, XSum) 达到52–56%。对话数据 (ShareGPT, Alpaca) 因对话中频繁出现重复短语，大量受益于自引用层级 (33–42%)。

![层级分布](report_e12/tier_distribution.png)

---

## 5. 原始参考余弦相似度

在应用差分编码之前，缓存参考激活值与真实激活值之间的原始余弦相似度衡量了DAG字典树预测的近似程度：

| 层级 | ctx = 512 (均值 / 最差最小值) | ctx = 2048 (均值 / 最差最小值) |
|------|-------------------------------|-------------------------------|
| 三元组 | 0.9554 / 0.7893 | 0.9601 / 0.7189 |
| 二元组 | 0.9124 / 0.6527 | 0.9172 / 0.5648 |
| 自引用 | 0.9436 / 0.8396 | 0.9594 / 0.7540 |

三元组参考最强 (均值余弦0.96)。二元组参考稍弱 (0.92)，因为上下文更短。自引用参考较强 (0.96)，因为它们来自同一请求。

这些原始相似度是"起点"——仿射变换 + 差分编码将剩余差距缩小到 > 0.999。

![各层级原始余弦](report_e12/raw_cosine_per_tier.png)

---

## 6. 重建质量

经过完整的编码/解码流水线后，重建质量以原始激活值和重建激活值之间的余弦相似度衡量：

### 整体指标

| 指标 | ctx = 512 | ctx = 2048 |
|------|-----------|------------|
| 余弦均值 | **0.99970** | **0.99967** |
| 余弦均值-最小值 | 0.99706 | 0.99653 |
| MSE均值 | 0.00130 | 0.00096 |
| MSE均值-最大值 | 0.283 | 0.282 |

### 各层级重建余弦 (ctx = 2048)

| 层级 | 均值 | 均值-最小值 |
|------|------|------------|
| 三元组 | 0.99962 | 0.99755 |
| 二元组 | 0.99920 | 0.99658 |
| 自引用 | 0.99960 | 0.99779 |
| 一元组 | **0.99998** | 0.99997 |

一元组实现最高重建质量，因为Int8量化 + FP16离群值编码本质上是无损的。差分编码层级 (三元组、二元组、自引用) 得益于仿射变换吸收了大部分参考值与真实值之间的差距，均达到 > 0.999。

![重建余弦](report_e12/reconstruction_cosine.png)
![重建余弦箱线图](report_e12/recon_cosine_boxplot.png)

---

## 7. 压缩比

| 指标 | ctx = 512 | ctx = 2048 |
|------|-----------|------------|
| **压缩比** | **2.47× ± 0.21×** | **2.77× ± 0.24×** |
| 原始FP16大小 | 1835 MB | 7340 MB |
| 压缩后大小 | 747 MB | 2666 MB |

### 各层级传输量分解 (ctx = 2048)

| 层级 | 传输量 (MB) | 占比 |
|------|-------------|------|
| 三元组 | 500.3 | 18.8% |
| 二元组 | 453.1 | 17.0% |
| 自引用 | 395.5 | 14.8% |
| 一元组 | 1316.8 | 49.4% |

一元组层级在传输字节中占主导地位，因为 (a) 它占34%的位置，且 (b) Int8编码的压缩效率低于Int4差分编码。提升n-gram覆盖率将直接减少一元组份额并提高压缩比。

### 各数据集压缩比 (ctx = 2048)

| 数据集 | 压缩比 | 覆盖率% |
|--------|--------|---------|
| TriviaQA | 3.04× | 79.2% |
| GSM8K | 2.96× | 76.2% |
| Alpaca | 2.88× | 72.1% |
| ShareGPT | 2.74× | 62.9% |
| wikitext | 2.70× | 62.9% |
| XSum | 2.58× | 56.2% |
| CNN/DM | 2.52× | 52.3% |

压缩比与覆盖率直接相关：更高的n-gram匹配率意味着更多的Int4差分编码位置和更好的压缩效果。

![压缩比](report_e12/compression_ratio.png)

---

## 8. 延迟分解

### 各阶段耗时 (含CPU/GPU重叠)

| 阶段 | ctx = 512 | ctx = 2048 |
|------|-----------|------------|
| Prefill前向传播 (GPU) | 836.3 ± 443.1 ms | 1515.7 ± 184.8 ms |
| 分类 (CPU → 重叠) | **0.04 ms** | **0.10 ms** |
| 差分编码 (GPU) | 7.4 ± 23.0 ms | 12.1 ± 20.0 ms |
| 自引用编码 (GPU) | 5.4 ± 38.5 ms | 4.2 ± 26.6 ms |
| 一元组编码 (GPU) | 9.0 ± 43.9 ms | 2.6 ± 17.9 ms |
| 表更新 (CPU → 重叠) | **0.01 ms** | **0.01 ms** |
| **总计** | **902.0 ms** | **1565.1 ms** |

### CPU/GPU重叠效果

| 操作 | 实际CPU耗时 | 可见延迟 | 节省 |
|------|-------------|----------|------|
| 分类 (ctx=512) | ~2 ms | 0.04 ms | **98%被隐藏** |
| 分类 (ctx=2048) | ~9 ms | 0.10 ms | **99%被隐藏** |
| 表更新 (ctx=512) | ~2.5 ms | 0.01 ms | **99.6%被隐藏** |
| 表更新 (ctx=2048) | ~9.5 ms | 0.01 ms | **99.9%被隐藏** |

两个CPU操作实际上是零开销的——完全被GPU前向传播和编码阶段分别隐藏。

### 编码开销

| | ctx = 512 | ctx = 2048 |
|--|-----------|------------|
| 总编码时间 | 21.8 ms | 18.9 ms |
| 占prefill比例 | 2.6% | 1.2% |

编码开销很小：**不到prefill时间的3%**。该开销需与传输节省进行权衡。

![延迟分解](report_e12/latency_breakdown.png)

---

## 9. 不同带宽下的端到端通信开销

### 开销模型

由于prefill前向传播在基线系统和我们的系统中完全相同，我们仅比较**通信开销**——即prefill之外将激活值传递到下一PP阶段所需的额外时间：

- **基线 (FP16)**: T\_transfer\_raw
- **本方案 (三元组流水线)**: T\_encode + T\_transfer\_compressed + T\_decode

其中 T\_transfer = 数据字节数 / 带宽，T\_encode 为发送端压缩开销，T\_decode 为接收端解压缩和重建开销。

### 参数

| | ctx = 512 | ctx = 2048 |
|--|-----------|------------|
| 原始FP16传输量 | 5120.0 KB | 20479.2 KB |
| 压缩后传输量 | 2084.7 KB | 7437.4 KB |
| 编码开销 | 21.8 ms | 18.9 ms |
| 解码开销 | 0.34 ms | 0.63 ms |

### 目标带宽下的结果

#### ctx = 512

| 带宽 | 基线 (ms) | 本方案 (ms) | 加速比 |
|------|-----------|-------------|--------|
| **200 Mbps** | 200.0 | 103.5 | **1.93×** |
| **500 Mbps** | 80.0 | 54.7 | **1.46×** |
| **1 Gbps** | 40.0 | 38.4 | **1.04×** |

#### ctx = 2048

| 带宽 | 基线 (ms) | 本方案 (ms) | 加速比 |
|------|-----------|-------------|--------|
| **200 Mbps** | 800.0 | 310.0 | **2.58×** |
| **500 Mbps** | 320.0 | 135.7 | **2.36×** |
| **1 Gbps** | 160.0 | 77.6 | **2.06×** |

在 **200 Mbps、ctx = 2048** 条件下，本方案将通信开销从 **800 ms降低到310 ms** (2.58倍加速)，每请求节省 **490 ms**。2.77倍压缩将传输时间从800 ms减少到291 ms，而编码 (18.9 ms) + 解码 (0.63 ms) 的总开销仅为很小的代价。

即使在 **1 Gbps** 下，系统对ctx = 2048仍提供 **2.06倍加速**，因为压缩比 (2.77×) 的收益远超19.5 ms的编解码开销。

在极高带宽 (> 10 Gbps，如NVLink或InfiniBand) 下，传输时间变得可忽略，编解码开销超过节省量。本系统专为带宽受限的跨节点场景设计。

![端到端延迟 vs 带宽](report_e12/e2e_latency_vs_bandwidth.png)
![加速比 vs 带宽](report_e12/speedup_vs_bandwidth.png)

---

## 10. 表增长轨迹

随着新请求引入未见过的三元组模式，DAG字典树持续增长：

| | ctx = 512 | ctx = 2048 |
|--|-----------|------------|
| 第0个请求后 | 461 tri / 418 bi | 1,636 tri / 1,400 bi |
| 第50个请求后 (预热结束) | 20,869 tri / 15,804 bi | 70,049 tri / 47,307 bi |
| 第99个请求后 | 39,188 tri / 28,198 bi | 126,147 tri / 79,695 bi |
| 第99个请求时内存 | 690 MB | 2,108 MB |

表在测试阶段持续增长——证实测试提示词确实是全新内容。增长速率随常见n-gram被覆盖而减缓，但在100个请求内未达到平台期。

![表增长](report_e12/table_growth.png)

---

## 11. 覆盖率增长趋势

下图展示n-gram覆盖率 (三元组 + 二元组 + 自引用) 在100个请求序列中的演变：

![覆盖率增长](report_e12/coverage_growth.png)

覆盖率在前20个请求中快速增长，随后增速趋于平缓。红色竖线标记预热边界 (第50个请求)。覆盖率在测试阶段随表增长继续提升。

---

## 12. 重建质量稳定性

余弦相似度在预热和测试阶段均保持稳定：

![余弦随请求变化](report_e12/cosine_over_requests.png)

---

## 13. 总结

| 指标 | ctx = 512 | ctx = 2048 |
|------|-----------|------------|
| 三元组匹配率 | 17.0% | 24.5% |
| 自引用匹配率 | 9.4% | 19.3% |
| 总n-gram覆盖率 | **48.3%** | **66.0%** |
| 重建余弦相似度 | **0.9997** | **0.9997** |
| 压缩比 | **2.47×** | **2.77×** |
| Prefill时间 | 836.3 ms | 1515.7 ms |
| 编码开销 | 21.8 ms | 18.9 ms |
| 分类 (重叠) | 0.04 ms | 0.10 ms |
| 表更新 (重叠) | 0.01 ms | 0.01 ms |
| 解码开销 | 0.34 ms | 0.63 ms |
| **通信加速 @ 200 Mbps** | **1.93×** | **2.58×** |
| **通信加速 @ 500 Mbps** | **1.46×** | **2.36×** |
| **通信加速 @ 1 Gbps** | **1.04×** | **2.06×** |

### 核心结论

1. **近无损压缩**: 余弦相似度0.9997——重建激活值与原始值几乎完全一致。

2. **2.5–3.0倍压缩**: 通过针对缓存n-gram参考的分层差分编码实现，在7个多样化数据集上使用全新未见提示词评估。

3. **零开销CPU操作**: 分类和表更新阶段通过线程级CPU/GPU重叠完全隐藏在GPU工作之后，可见延迟贡献 < 0.15 ms。

4. **显著的通信加速**: 通过隔离通信开销 (排除两个系统共有的prefill)，三元组流水线在200 Mbps–1 Gbps下对ctx = 2048实现 **2.06–2.58倍加速**，每请求最多减少490 ms的跨阶段传输延迟。

5. **跨带宽范围有效**: 即使在1 Gbps (典型InfiniBand子网或高端以太网) 下，系统对长上下文仍提供2.06倍通信加速。在200 Mbps (云VPC、广域网链路) 下，加速比达到2.58倍。

6. **覆盖率随使用增长**: DAG字典树持续学习新模式。在ctx = 2048下经过50次预热请求后，覆盖率达到66%并继续提升。特定领域工作负载 (GSM8K, TriviaQA) 可达79%覆盖率和3.0倍压缩。

---

*所有数据由E12实验流水线生成。图表位于 `report_e12/`，原始parquet数据位于 `results_trigram_pipeline/`。*
